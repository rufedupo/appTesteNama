<p id="notice"><%= notice %></p>

<h1>Fornecedores</h1>

<table class="table table-striped">
    <thead>
        <tr>
          <th>Nome</th>
          <th>Endereço</th>
        </tr>
    </thead>
    <tbody>
      <% @providers.each do |provider| %>
        <tr>
          <td><%= provider.name %></td>
          <td><%= provider.address %></td>
        </tr>
      <% end %>
    </tbody>
</table>